import 'package:flutter/material.dart';

class JoinQuestScreen extends StatelessWidget {
  const JoinQuestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Join Quest')),
      body: const Center(child: Text('Join Quest Screen (Placeholder)')),
    );
  }
}
