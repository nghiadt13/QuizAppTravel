rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all collections (demo purposes)
    match /{document=**} {
      allow read, write: if request.auth != null;
      allow read: if true; // Allow public reads for quests/leaderboards
    }
  }
}
